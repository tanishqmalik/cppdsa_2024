#include <iostream>
using namespace std;

int main(){
    int arr[1000];
    int i,j,limit;

    for(i=0; i<limit; i++){
        cin>>arr[i];
    }

    // int size = sizeof(int)/sizeof(arr[0]);

    int right = limit-1;
    int left = 0;

    while(left<=right){
        
    }
}